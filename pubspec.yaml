name: resep_masakan_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Perbaiki format sdk

dependencies:
  flutter:
    sdk: flutter
  
  # SEMUA PACKAGE HARUS DI SINI (sejajar dengan flutter:)
  http: ^1.1.0
  provider: ^6.0.5
  cached_network_image: ^3.3.0
  shared_preferences: ^2.2.0
  flutter_staggered_grid_view: ^0.7.0
  google_fonts: ^5.1.0
  flutter_rating_bar: ^4.0.1
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: fonts/Poppins-Regular.ttf
  #       - asset: fonts/Poppins-Bold.ttf
  #         weight: 700